# Short guide to using git
## Clone a github repo to local machine

```
git clone <URL>
```

## Push changes to github repo

```
git add .
git commit -m "<Your description>"
git push origin master
```
